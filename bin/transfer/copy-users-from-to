#!/bin/bash
set -e

if [ "$#" -ne 1 ]; then
    echo "Usage: $0 user@old_droplet_host user@new_droplet_host"
    exit 1
fi

# TODO copy users and their .ssh/authorized_keys, setting correct permissions
