version: "3.2"

services:
  app:
    ports:
      - "3000:3000"
    command:
      - /bin/sh
      - -c
      - |
        bundle exec rake db:migrate ids:update
        bundle exec unicorn -c config/containers/unicorn.rb -E $RAILS_ENV
  db:
    ports:
      - "5432:5432"
